"""
Utilities and one-off scripts for the BladderDIVE project.

This file makes the `scripts/` directory importable as a Python package so that
you can do:

```python
from scripts import napari_utils
```

from interactive environments like the Napari console, as long as the project
root is on `sys.path`.
"""

